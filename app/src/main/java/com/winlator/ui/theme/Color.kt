package com.winlator.ui.theme

import androidx.compose.ui.graphics.Color

// Used only by Theme.kt and ThemePreset.kt to build ColorScheme objects.
// Never import these directly in composables — use MaterialTheme.colorScheme.* instead.
val Surface          = Color(0xFF2A2A2A)
val OnSurface        = Color(0xFFE0E0E0)
val OnSurfaceVariant = Color(0xFFAAAAAA)
val DividerColor     = Color(0xFF404040)
